const subS = require('./subStack');
subS.mainFunction();


// Esto es conocido como el substack pattern
// Solo se expone una funcionalidad del módulo dejando el resto privado
// Se puede hacer con clases o con instacias